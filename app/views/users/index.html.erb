<p id="notice"><%= notice %></p>

<h1>Users</h1>

<table class="table">
  <thead>
    <tr>
      <th>Email</th>
      <th>A faire</th>
      <th>Fait</th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to user, user %></td>
        <td><%= user.candidates_attributed.count %></td>
        <td><%= user.candidates_evaluated.count %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= simple_form_for(baccalaureat) do |f| %>
  <%= f.input :title %>
  <%= f.association :parent %>
  <%= f.input :quota %>
  <%= f.input :evaluation_bonus %>
  <%= f.submit 'Enregistrer', class: 'btn btn-primary' %>
<% end %>

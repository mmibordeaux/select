<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= yield(:title).presence || 'Select' %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= favicon_link_tag 'favicon.png', :rel => 'shortcut icon' %>
    <%= javascript_include_tag 'application' %>
  </head>
  <body>
    <%= render 'nav' if current_user %>
    <div class="jumbotron">
      <div>
        <h1><%= yield(:title).presence %></h1>
      </div>
    </div>
    <main class="container-fluid">
      <p><%= notice %></p>
      <%= render 'breadcrumb' %>
      <%= yield %>
    </main>
    <%= render 'footer' %>
  </body>
</html>
